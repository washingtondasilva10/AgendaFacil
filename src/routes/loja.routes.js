const express=require('express');const r=express.Router();const c=require('../controllers/loja.controller');const {verificarToken}=require('../middlewares/auth');
r.get('/publico/:empresa',c.listarPublico);r.get('/',verificarToken,c.listar);r.post('/',verificarToken,c.criar);r.put('/:id',verificarToken,c.atualizar);r.delete('/:id',verificarToken,c.remover);r.post('/agendamento/:agendamento/finalizar',verificarToken,c.finalizarVenda);r.post('/agendamento/:agendamento/cancelar',verificarToken,c.cancelarReserva);r.get('/financeiro/resumo',verificarToken,c.financeiro);module.exports=r;
